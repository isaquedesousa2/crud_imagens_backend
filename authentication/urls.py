# from rest_framework import routers
# from .views import UserAuthenticationViewSet

# router = routers.DefaultRouter()

# router.register('auth', UserAuthenticationViewSet, basename='auth')

# urlpatterns = router.urls
